CREATE TABLE bar (
        bar_id integer not null,
        bar_name varchar(255),
        PRIMARY KEY (bar_id)
    );

CREATE TABLE bar_aud (
        bar_id integer not null,
        rev integer not null,
        revtype tinyint,
        bar_name varchar(255),
        PRIMARY KEY (bar_id, rev)
    );
    
CREATE TABLE revinfo (
        rev integer generated by default as identity,
        revtstmp bigint,
        PRIMARY KEY (rev)
    );
    
ALTER TABLE bar_aud 
        add FOREIGN KEY (rev) 
        references revinfo;
        
ALTER TABLE bar_aud 
        add FOREIGN KEY (rev) 
        references revinfo;